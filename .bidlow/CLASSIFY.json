{
  "_comment": "STAGE 2 — CLASSIFY. Types declared 2026-08-20 from evidence in the codebase, NOT from assumption: the repo sends on clients' behalf via sequences and mailboxes (email_infrastructure), holds contacts/clients/campaigns with membership-scoped access (crm, multi_tenancy, identity_auth), advances sequences on a schedule via GitHub Actions (background_processing), and carries a do-not-contact suppression list (regulated_data).",
  "_answers_note": "2026-08-22: the questions tagged `research` in standards/saas-types.json are drafted below from DOMAIN-BRIEF-cold-email.md (written 30 July 2026, cited) and from what the code actually does, verified by reading it on this date. The questions tagged `decision` and `fact` are DELIBERATELY still blank — the catalogue says only Greg can answer those, and inventing them here is the exact failure this stage exists to prevent. They are listed for him in the session report. A partially answered domain still blocks, and that is correct.",
  "types": [
    "cold_outreach"
  ],
  "declared_by": "Cowork session, 2026-08-20, from code evidence",
  "answers": {
    "email_infrastructure": {
      "0": {
        "answer": "THE CLIENT owns it. ODoutreach does not send from an OpensDoors domain on the live path: it sends as the client, through the client's OWN connected Microsoft 365 or Google Workspace mailbox via Graph / Gmail API, so the From: address, the sending domain and its reputation all belong to the client. OpensDoors owns only the app domain (opensdoors.bidlow.co.uk) — which is precisely why putting a link on that domain into a client-From: email was the quarantine root cause, and why it has now been removed. CONSEQUENCE: reputation damage lands on the CLIENT's ordinary business email, not on a disposable OpensDoors sending domain. That is exactly why the volume cap and the warm-up ramp are not optional extras. DRAFTED FROM THE RECORD FOR GREG TO CONFIRM rather than asked cold — this is his stated architecture, restated from the code.",
        "confidence": "SP",
        "source": "Code read 2026-08-22/23: src/server/email/outbound/execute-one.ts (sendViaConnectedMailboxOrFail, no ESP fallback), src/lib/clients/client-link-domain.ts, src/config/production-platform-rules.ts ('reputation and delivery are the customer's mailbox provider'); DOMAIN-BRIEF-cold-email.md sec.0",
        "expires": "2027-02-23"
      },
      "1": {
        "answer": "NO — and that refusal is the defining constraint of this product, not an obstacle to route around. Greg has stated the OpensDoors owner does not want to require clients to add DNS records, and has called it the main open problem on the project. THE ENGINEERING ANSWER IS feat/zero-dns-send-profile, MERGED 2026-08-22. It works because sending through the client's own Microsoft 365 / Google Workspace mailbox means SPF, DKIM and DMARC ALREADY PASS on the client's own domain — the provider signs the mail, exactly as it signs their ordinary Outlook or Gmail. No customer DNS change is needed for AUTHENTICATION. What actually broke was never authentication: it was LINK ALIGNMENT — an email From: the client's domain carrying an unsubscribe link on opensdoors.bidlow.co.uk, which is the textbook phishing pattern. The merged branch removes the app-domain fallback entirely, stops importing resolvePublicBaseUrl into that module so it cannot return by accident, and falls back to a mailto opt-out on the sending mailbox, so a compliant send now carries NO host other than the sender's own. RESIDUAL, stated plainly: a client who wants HOSTED one-click unsubscribe or click tracking still needs one CNAME for an aligned link domain (go.<their-domain>). Zero-DNS buys correct, authenticated, non-phishing sends; it does not buy hosted links. DRAFTED FOR GREG TO CONFIRM.",
        "confidence": "SP",
        "source": "DOMAIN-BRIEF-cold-email.md sec.0 (30 Jul 2026); merged branch feat/zero-dns-send-profile commits 36a1fdf, 83b7170, c6a4a83, a8d777c; code read 2026-08-22: src/lib/unsubscribe/one-click-readiness.ts (resolveUnsubscribeRail), src/server/email-sequences/send-introduction.ts",
        "expires": "2027-02-23"
      },
      "3": {
        "answer": "30 days minimum from zero, per mailbox. Researched ramp: week 1 at 5-10/day to highly engaged recipients only, week 2 at 20-30, week 3 at 50-75 rising ~15% every 2-3 days, week 4 at 100-150; normal volume only after that, and only while monitoring. ENFORCED IN CODE, not by an operator: src/lib/mailboxes/mailbox-warmup.ts computes an effective daily cap from mailbox age (WARMUP_BASE_CAP 5, +5 every 5 days) and send-introduction.ts and controlled-pilot-send.ts both call effectiveDailyCap before dispatch. It is gated by env MAILBOX_WARMUP_RAMP === 'on'; with the flag off the steady cap applies and there is no ramp. CONFIRMED 2026-08-23: MAILBOX_WARMUP_RAMP is set to 'on' in Azure App Service config on app-opensdoors-outreach-prod, read directly from the live setting. The enforcement half of this answer is therefore evidenced, not assumed.",
        "confidence": "SP",
        "source": "DOMAIN-BRIEF-cold-email.md sec.2 'Warm-up ramp - 30 days minimum, from zero' (30 Jul 2026); code read 2026-08-22: src/lib/mailboxes/mailbox-warmup.ts, src/server/email-sequences/send-introduction.ts; Azure App Service setting read 2026-08-23: MAILBOX_WARMUP_RAMP=on",
        "expires": "2027-01-31"
      },
      "4": {
        "answer": "ODoutreach does not send through a bulk ESP on the live path: it sends as the client, through their own connected Microsoft 365 or Google Workspace mailbox (Graph / Gmail API), so the governing limit is the MAILBOX provider's, not an ESP quota. The product's own enforced ceiling is 5 mailboxes per client at 30 sends/mailbox/day, which is deliberately tighter than the researched industry norm of 4-6 mailboxes at 40-50/day (~200/day/domain). CAVEAT, stated rather than hidden: Microsoft's and Google's own published per-mailbox limits were NOT re-verified against their primary documentation in this session. The figure above is the product's self-imposed cap, which is the number that actually binds today, but the provider ceiling behind it is owed a dated primary-source check before volume rises.",
        "confidence": "SP",
        "source": "DOMAIN-BRIEF-cold-email.md sec.2 'Volume architecture' (30 Jul 2026); code read 2026-08-22: src/server/mailbox/sending-policy.ts (DEFAULT_MAILBOX_DAILY_SEND_CAP, MAILBOX_DAILY_CAP error)",
        "expires": "2026-11-22"
      },
      "5": {
        "answer": "Unsubscribes: immediate and durable. A hosted one-click opt-out is minted only on a client-aligned link domain; with none configured the send carries a mailto opt-out on the sending mailbox, and replies are ingested and classified by classifyOptOutReply, which writes a suppression row. evaluateSuppression is re-checked at dispatch, so an opt-out registered between queue and send still blocks. Complaints: a spam-complaint path writes a suppression row (audit L2). Bounces: THIS IS THE WEAK LEG. Hard bounces suppress via suppressRecipientForHardBounce (append-only, idempotent) and BOUNCE_SUPPRESSION_ENABLED is on in production, but that only acts on a bounce someone has DETECTED. For the live mailbox path there is no provider delivery webhook; detection is by NDR/DSN bounce-back parsed out of the connected mailbox during inbox sync (src/server/mailbox/bounce-detection.ts, wired into mailbox-inbox-sync.ts) - and that is gated by MAILBOX_BOUNCE_DETECTION_ENABLED, which DEFAULTS OFF in code and was undocumented (added to .env.example 2026-08-23). CORRECTION, 2026-08-23: the flag is SET TO true in Azure App Service config on app-opensdoors-outreach-prod, verified by reading the live setting, as are MAILBOX_COMPLAINT_DETECTION_ENABLED and BOUNCE_SUPPRESSION_ENABLED. So the earlier inference - that a reported 0% bounce rate across ~1,209 sends was explained by the detector being switched off - is WRONG and is withdrawn. Detection is on. The 0% is therefore either genuine (hand-checked lists really do bounce near zero) or the detector is not matching NDRs back to the sent rows. That cannot be settled without production data, so it stays an OPEN question and the volume cap stands on the strength of the uncertainty rather than on a diagnosis. Timing: suppression is immediate on detection; detection is bounded by the reply-sync cron, which runs every 15 minutes on weekdays.",
        "confidence": "SP",
        "source": "Code read 2026-08-22: src/server/outreach/suppression-guard.ts, src/server/mailbox/bounce-detection.ts (isMailboxBounceDetectionEnabled, DEFAULT OFF), src/server/mailbox/mailbox-inbox-sync.ts, src/server/email/bounce-suppression.ts, .github/workflows/sync-replies.yml; thresholds from DOMAIN-BRIEF-cold-email.md sec.1 (below 2% bounce, below 0.1% complaint); Azure App Service settings read 2026-08-23: MAILBOX_BOUNCE_DETECTION_ENABLED=true, MAILBOX_COMPLAINT_DETECTION_ENABLED=true, MAILBOX_WARMUP_RAMP=on",
        "expires": "2026-11-22"
      },
      "7": {
        "answer": "NO. NOTHING STOPS SENDING AUTOMATICALLY ON A REPUTATION OR BOUNCE SIGNAL. A person has to notice. Verified by reading every consumer of the metric: bounceRate is computed in src/lib/reports/outreach-metrics.ts and rendered in exactly two places — the client Activity page and the Reports page — and no send path anywhere reads it. There is no threshold constant, no scheduled reputation check, no auto-pause. What DOES stop a send is all per-recipient or per-mailbox, and none of it is rate-aware: suppression / DNC / unsubscribe / hard-bounce on that ONE address; the per-mailbox daily cap; the warm-up ramp by mailbox age; the manual isSendingEnabled toggle; and pausing or archiving the client. So a list bouncing at 30% is stopped only by whoever happens to look at the Activity page. THIS IS THE REASON THE VOLUME CAP EXISTS: with no automatic circuit breaker, a low absolute ceiling is the only thing bounding the damage between a problem starting and a human seeing it. An automatic halt on a rolling bounce/complaint threshold is the obvious next control, and it is NOT built.",
        "confidence": "V",
        "source": "Code read 2026-08-23: exhaustive grep of every bounceRate consumer — src/lib/reports/outreach-metrics.ts, src/app/(app)/reporting/page.tsx, src/app/(app)/clients/[clientId]/activity/page.tsx, all display-only; no threshold constant anywhere in src/server/email, src/server/mailbox or src/lib/clients; stop conditions enumerated from src/server/mailbox/sending-policy.ts and src/lib/clients/client-send-governance.ts",
        "expires": "2027-02-23"
      },
      "__specialist_review": {
        "researched_from": [
          "DOMAIN-BRIEF-cold-email.md (this repo, written 30 July 2026) — the dated, cited domain brief for cold email, covering authentication, volume architecture, the warm-up ramp, and the bounce/complaint thresholds.",
          "Its cited sources: redsift.com/guides/bulk-email-sender-requirements; powerdmarc.com/bulk-email-sender-requirements/; hunter.io/cold-email-guide/dedicated-cold-email-domain; growleads.io/blog/subdomain-for-cold-email-protect-main-domain/",
          "Direct reading of the sending path in this repo on 2026-08-22: mailbox-warmup.ts, sending-policy.ts, send-introduction.ts, execute-one.ts, bounce-detection.ts, mailbox-inbox-sync.ts, suppression-guard.ts"
        ],
        "date": "2026-08-22",
        "note": "NO human email-deliverability specialist has reviewed this. This record is research provenance, not a specialist sign-off, and it should not be read as one. The unverified item is named in Q5: Microsoft's and Google's own published per-mailbox sending limits were not re-checked against their primary documentation in this session."
      }
    },
    "regulated_data": {
      "0": {
        "answer": "Mapped from prisma/schema.prisma and the mailbox sync path on 2026-08-23. (a) PROSPECT IDENTITY AND CONTACT DETAIL, per Contact: email, fullName, firstName, lastName, company, title, linkedIn, mobilePhone, officePhone, location, city, country, industry, emailDomain, source. Mirrored and DEDUPLICATED ACROSS ALL CLIENTS in ContactUniverse (emailNormalized, linkedinUrlNormalized, mobilePhoneNormalized, officePhoneNormalized) — the warehouse is cross-tenant by design, so one prospect may appear under several clients. (b) ENRICHMENT BOUGHT FROM A THIRD PARTY: RocketReachEnrichment holds a raw JSON payload from an external data broker. (c) OUTBOUND CONTENT: OutboundEmail holds toEmail, toDomain, fromAddress, subject and bodySnapshot — the FULL sent body — plus provider ids and failure reasons. (d) INBOUND CONTENT, the widest category: InboundMailboxMessage holds fromEmail, toEmail, subject, snippet, bodyPreview and bodyText — the FULL TEXT of messages fetched from the client's connected mailbox — and InboundReply holds fromEmail, toEmail, subject, snippet, bodyPreview. (e) STAFF: StaffUser email, displayName and Entra object id. (f) SUPPRESSION: SuppressedEmail / SuppressedDomain — a record that a named person asked not to be contacted, which is itself personal data, and the one category that must OUTLIVE erasure of the rest. SPECIAL CATEGORY DATA — the honest risk: nothing in (d) is filtered by content. A prospect replying to a cold email can disclose anything — ill health, a bereavement, trade-union or political affiliation — and it is stored verbatim in bodyText, with no special handling, no flagging, and no retention limit (see Q4: there is no retention policy at all). The system does not SEEK special-category data; it will nonetheless accumulate it. No children's data is sought — the product is B2B outreach to named business contacts.",
        "confidence": "V",
        "source": "Direct schema read 2026-08-23: prisma/schema.prisma models Contact (L696), ContactUniverse (L754), RocketReachEnrichment (L880), OutboundEmail (L1279), InboundMailboxMessage (L601), InboundReply (L1410), StaffUser (L253), SuppressedEmail (L925), SuppressedDomain (L938); sync path src/server/mailbox/mailbox-inbox-sync.ts",
        "expires": "2027-02-23"
      },
      "1": {
        "answer": "UK PECR Regulation 22 plus UK GDPR. The distinction that governs is the recipient's LEGAL STRUCTURE, not whether they are 'a business': limited companies, LLPs and public bodies are corporate subscribers and need no prior consent, while sole traders, unincorporated partnerships and personal addresses are individual subscribers and need consent or soft opt-in exactly as consumers do. A one-person limited company falls under the carve-out; a sole trader does not. Always required regardless: truthful sender identification and a functional opt-out honoured across all future campaigns. UK GDPR sits on top - a work email address is personal data, and the lawful basis for B2B outreach is normally legitimate interests, which requires a DOCUMENTED balancing assessment (genuine purpose, proportionality, interest outweighing the recipient's rights). KNOWN PRODUCT GAP, named in the brief and still true in the code on 2026-08-22: the contact-import pipeline cannot distinguish a corporate subscriber from an individual subscriber, so the PECR consent question cannot currently be answered per recipient.",
        "confidence": "V",
        "source": "DOMAIN-BRIEF-cold-email.md sec.3 'The legal position - UK' (researched and cited 30 Jul 2026), citing PECR Reg 22 and UK GDPR via ea.partners/post/is-cold-email-legal-in-the-uk and salespeople.co.uk/explained/cold-email-pecr-regulation-22. Not legal advice; the brief itself says confirm with a solicitor before relying on it commercially.",
        "expires": "2027-01-30"
      },
      "2": {
        "answer": "UK/EU. The data is prospect contact data on UK businesses processed under UK GDPR, and the deployment is Azure App Service + Azure Database for PostgreSQL in the OpensDoors production resource group (rg-opensdoors-outreach-prod). UK GDPR does not mandate UK-only residency, but transfers outside the UK need a transfer mechanism, so the practical rule is: keep the database in the UK South / UK West region and do not introduce a processor that moves it. CAVEAT: the Azure region actually configured for the production database was NOT verified in this session - it is an Azure portal check, and it decides whether this answer is a description or a claim. Data residency is a listed one-way door, so it should be confirmed rather than assumed.",
        "confidence": "SP",
        "source": "Deployment facts from CLAUDE.md (app-opensdoors-outreach-prod, rg-opensdoors-outreach-prod) and DOMAIN-BRIEF-cold-email.md sec.3; region NOT verified 2026-08-22",
        "expires": "2026-11-22"
      },
      "3": {
        "answer": "There is no retention policy expressed in the code. Nothing in the schema or in any scheduled job deletes prospect data on age: Contact, OutboundEmail and InboundReply rows persist indefinitely, and the only deletion path is the super-admin workspace soft-delete/purge. Suppression must outlive the contact record it refers to, or deleting a contact silently un-suppresses them. NOTE, and this cuts against the usual 'suppression is append-only' shorthand: only the hard-bounce path is append-only. Google-Sheet-sourced suppression is REPLACE-ON-SYNC - suppression-sync.ts deleteMany's every SuppressedEmail and SuppressedDomain row for that sourceId and rewrites from the sheet, so an address removed from the client's sheet becomes sendable again. There is a shrink warning, but it warns rather than blocks. What SHOULD force deletion under UK GDPR storage limitation is a defined purpose-linked period (prospect data kept no longer than the outreach purpose needs) plus erasure on request; neither is implemented. Retention is a listed one-way door and it is currently UNSETTLED - which is a finding, not an answer, and it is why this domain still blocks.",
        "confidence": "SP",
        "source": "Code read 2026-08-22: prisma/schema.prisma (no TTL/retention fields), .github/workflows (no purge job), src/server/outreach/suppression-guard.ts (append-only suppression); storage-limitation principle per UK GDPR Art.5(1)(e) as summarised in DOMAIN-BRIEF-cold-email.md sec.3",
        "expires": "2026-11-22"
      },
      "__specialist_review": {
        "researched_from": [
          "DOMAIN-BRIEF-cold-email.md sec.3 'The legal position — UK' (written 30 July 2026), covering PECR Regulation 22, the corporate-vs-individual subscriber distinction, and the UK GDPR legitimate-interests basis.",
          "Its cited sources: ea.partners/post/is-cold-email-legal-in-the-uk; salespeople.co.uk/explained/cold-email-pecr-regulation-22",
          "Direct reading of the data model and deletion paths in this repo on 2026-08-22: prisma/schema.prisma, suppression-guard.ts, google-sheets/suppression-sync.ts, .github/workflows/"
        ],
        "date": "2026-08-22",
        "note": "NO solicitor has reviewed this. The brief itself says to confirm with a solicitor before relying on it commercially, and that caveat stands. Two substantive gaps are recorded rather than papered over: the import pipeline cannot distinguish a corporate subscriber from an individual subscriber (so PECR consent cannot be decided per recipient), and there is no retention policy in code."
      }
    },
    "multi_tenancy": {
      "1": {
        "answer": "APPLICATION CODE ONLY, and it does not isolate between staff. There is no Postgres row-level security anywhere in the schema or migrations. The single read-side control is getAccessibleClientIds (src/server/tenant/access.ts), which DISCARDS its `staff` argument and returns every client with deletedAt: null - so the only wall it builds is live-vs-soft-deleted, not tenant-vs-tenant. ClientMembership exists and is written by the membership actions, but no read path consults it; only three mutator-access helpers (email-sequences, email-templates, mailbox-identities) read it, and those gate WRITES. Proven live on 2026-08-22: a staff user who is a member of Client B only opened Client A's workspace overview, its activity feed (real prospect address plus subject line) and its outbound email detail, all HTTP 200 with full data. The unit test that appears to cover this (src/server/tenant/access.test.ts) mocks prisma.client.findMany, so it never sees the where clause that is the whole control and stays green with isolation on or off. e2e/cross-tenant.spec.ts is the only thing that detects it, and it is RED.",
        "confidence": "V",
        "source": "Code read and live HTTP probes 2026-08-22 against a local production build: src/server/tenant/access.ts, src/server/tenant/access.test.ts, prisma/schema.prisma (no RLS), e2e/cross-tenant.spec.ts run output (2 positive controls pass, 3 disclosure assertions fail)",
        "expires": "2027-02-22"
      },
      "3": {
        "answer": "EVERY active staff member can see across every live tenant, and it is NOT logged per access. This is by design, not a defect: ODoutreach was built as an internal OpensDoors tool where all staff are one company, and in-account roles were deliberately removed in 2026-06. GREG'S DECISION, 2026-08-23: this design stands and the 64-call-site isolation project is NOT to be started. The pilot is separated at the DEPLOYMENT level instead - one instance per client organisation - so no staff member reads another organisation's prospects. CONSEQUENCE TO STATE PLAINLY, and it is a commercial constraint rather than a bug: selling a SHARED instance to two clients who must not see each other is a REBUILD, not a feature. Nothing about the current product should be sold on a claim of tenant isolation between staff users.",
        "confidence": "SP",
        "source": "Greg's decision recorded 2026-08-23 (MONDAY-2-EXECUTE.md sec.1); design intent documented in the docstring of src/server/tenant/access.ts; role removal recorded in .bidlow/STATE.md",
        "expires": "2027-02-22"
      },
      "4": {
        "answer": "A tenant is SOFT-deleted: Client.deletedAt is stamped, which removes it from getAccessibleClientIds and therefore from every normal read path, and the capability is gated on the per-account isSuperAdmin flag (canDeleteWorkspace), not on a role enum or an email string. A separate explicit hard purge follows. Everything is retained until that purge - contacts, outbound emails, replies - so a soft-delete is recoverable and is NOT an erasure under UK GDPR. That matters because there is no retention policy in code either (see regulated_data Q4), so 'deleted' currently means 'hidden from staff', and should not be described to a client as anything stronger. FOR GREG TO CONFIRM: whether that is the intended contractual meaning of deleting a workspace.",
        "confidence": "SP",
        "source": "Code read 2026-08-22/23: src/server/tenant/access.ts (canDeleteWorkspace, deletedAt filter), prisma/schema.prisma (Client.deletedAt, F2 comments), listSoftDeletedClients super-admin gating",
        "expires": "2026-11-23"
      }
    }
  },
  "_still_blank_and_why": {
    "email_infrastructure": {
      "2": "decision — subdomain vs root for sending. ONE-WAY DOOR. Greg only. Note that the zero-DNS architecture partly pre-empts it: sends leave from the client's EXISTING mailbox on their root domain, so choosing a sending subdomain would mean provisioning NEW mailboxes, not reconfiguring these.",
      "6": "decision — separate tracking domain. Greg only. Tied to the residual in Q2: a single aligned link domain (go.<client-domain>) would serve both hosted unsubscribe and click tracking, and needs one CNAME from the client."
    },
    "regulated_data": {
      "4": "fact — DPAs. AN OWED OBLIGATION, NOT PAPERWORK: UK GDPR Art.28 requires a written contract with every processor BEFORE they process personal data on your behalf. The chain is Microsoft (Azure host + database + Graph transport), Google (Workspace/Gmail transport + Sheets suppression sync), Sentry (error monitoring, which can capture request context), RocketReach (a data broker — which also raises a CONTROLLER question about the lawful basis of the data being bought), and Resend on the legacy fallback path. Greg must confirm each. This is outstanding BEFORE real prospect data is processed."
    },
    "multi_tenancy": {
      "0": "decision — can one person belong to more than one tenant. The SCHEMA allows it (ClientMembership is a join table, unique on staffUserId+clientId), but the intended rule is Greg's, and today the question is moot because everyone sees everything regardless of membership.",
      "2": "decision — can a record ever move between tenants. Not evidenced either way in the code; Greg's call."
    }
  }
}