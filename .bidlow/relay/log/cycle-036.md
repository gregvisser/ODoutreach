# Cycle 36 - finished

Work happened. Evidence: a git ref moved, so something was committed; the working tree changed, so files were edited.

Started 2026-08-27 06:57:09, took about 32.3 minutes.
How it ended: exit code 0.

Evidence checked: git refs on every branch, the working tree, and these
files named in the brief: bidlow/relay/QUEUE.md

## What it was asked to do

# Cycle 36 - queue item 28

This brief was written by the relay itself, off the top of QUEUE.md. Greg has
not read it. If it is wrong, say so in your log rather than working around it,
and correct QUEUE.md.

## The item, verbatim from the queue

> **TWO DO-NOT-CONTACT SHEET SYNCS ARE FAILING RIGHT NOW, AND ONE OF THEM IS THE BIGGEST SENDER.** On /suppression, live, 2026-08-26: **Train Hugger ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â Whole domains ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â 'Last sync failed'** (serving 373 stale rows) and **Pareto FM ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â Whole domains ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â 'Last sync failed'** (0 rows, so Pareto FM currently has NO domain blocklist at all). Train Hugger is the client with 743 sends. This is a safety problem before it is a UI problem: a blocklist that silently stopped updating is how a do-not-contact address gets contacted, which is the exact failure Greg is being paid to eliminate. The on-screen hint says to check the tab name and range. Find out why each one fails, fix what can be fixed from this side, and if it needs the customer to reshare the sheet, say so plainly in one line Greg can forward. Do not hide a failure behind a retry. **NOTE from cycle 23 (not this row's owner): the work IS committed as `bd8bb21` on branch `fix/dnc-sheet-sync-silent-failure` and PR #250 is OPEN Ã¢â‚¬â€ but its CI run `33017266904` FAILED. The commit is safe; the branch was not lost in cycle 23's tree churn. Whoever picks this up: start by reading that failed run, not by rebuilding.**

## The one rule

THE HARD RULE, and it is not negotiable:
Real email may be sent, and data deleted, ONLY for the `bidlowai` client.
Every other client may be built on, tested and measured. Nothing leaves the
building for them. This is enforced in `autonomous-actor-guard.ts`, not by
your good intentions. If a task seems to need a real send for anyone else,
that task is wrong - stop and write down why.

## Before you touch anything, write these four things down

1. **The files you are going to change.** Name them. If you cannot yet, your
   first job is to find out, and that reconnaissance IS the cycle.
2. **The red-first test.** Name the test file and what it asserts. Watch it FAIL
   before you make it pass. If the behaviour cannot go red first, say why, and
   prove the test is capable of failing by deliberately breaking the code and
   showing the red - that is this repository's established substitute.
3. **What "done" looks like** for this item, in one sentence a non-coder can check.
4. **What you must NOT touch.** Anything outside the files in (1).

## The rules that apply to every cycle

* Do not stall on a question. Decide, record the decision and why, and continue.
  If the decision is genuinely Greg's - money, a client relationship, an
  irreversible one-way door - stop and write down the question instead.
* Gates before you claim anything: `npm run lint`, `npm run typecheck`,
  `npm test`. Show the real output. A gate you did not run is not met.
* Commit and push when confident. Branch protection is ON, so it is
  branch -> PR -> green CI -> merge. Never push straight to `main`.
* If you deploy, verify the running commit by HASH against the DIRECT App
  Service URL (`app-opensdoors-outreach-prod.azurewebsites.net`), never the
  CDN-cached custom domain, and never liveness alone.
* Production migrations are real. `PRODUCTION_PRISMA_MIGRATE` is true, so
  merging a migration applies it to the live client database.
* When you finish, update this item's row in `.bidlow/relay/QUEUE.md` to
  `DONE 36`, or back to `TODO` with a note if you could not do it.
* Do NOT write the next NEXT.md. The watcher does that. One cycle, one item.

## Assume the seventh exists

QUEUE.md records six instances this week of something built, wired, reporting
success, and never firing. It is the defect this project is worst at by a wide
margin. Whatever you build this cycle, prove it FIRES - not that it exists.


## What it did

Done. Here's what happened.

## The CI failure was a stale base, not the work

The red test was `relay/queue-parser.test.ts` — a file this branch never touched. `main` had since fixed it (`40b6fbe`, which adds `RELAY_ALERT_SUPPRESS` so the relay self-test doesn't send real email during `npm test`). I rebased `bd8bb21` onto current main; nothing else changed. Cycle 23's note was right to say don't rebuild.

Gates on the rebase: **lint 0, typecheck 0, 2562 tests green**. Merged as `789e780`, deployed, verified by hash on the direct App Service URL.

## I proved it fires

I did not stop at "the tests pass". I dispatched the real workflow against the live build (run `33045022987`). It went **red** at the `Fail run — PARTIAL` step and emitted three annotations. I read them back through the same check-runs endpoint `ops-alert.ts` uses, then rendered the actual email:

```
ODoutreach PARTIAL — reply & do-not-contact sync partly failed

  Sync replies: PARTIAL — part of it failed, with no count reported (12 runs)
      do-not-contact sheet sync: 2 of 34 sheet(s) failed to update
      do-not-contact sheet: Train Hugger — Whole domains: … This Sheet's tabs are: "Domains", "Company Names".
      do-not-contact sheet: Pareto FM — Whole domains: … This Sheet's tabs are: "Domains".
```

I also broke `alert-copy.ts` deliberately first and watched the email degrade to *"part of it failed, with no count reported"*, naming neither client — the exact silent shape. Then reverted.

## Why each one fails — answered

**Neither Sheet has a tab called `Sheet1`, and both sit on the default range `Sheet1!A1:Z50000`.** Train Hugger's tabs are `Domains` and `Company Names`; Pareto FM's is `Domains`.

**No customer needs to reshare anything** — a sharing problem answers 403, and these sheets read fine. Nothing to forward. This is a one-field fix on our side, staff-editable in the UI:

> On each client's suppression tab, set the Whole-domains range to `Domains!A1:Z50000`.

I did **not** make that change myself. It's a live blocklist config for a non-`bidlowai` client, I have no staff session, and the alternative is a direct production DB write — which is precisely what the working agreements gate. It's 30 seconds each in the UI and it's yours to click.

## One correction I did make

The test fixture described itself as *"the annotations the fixed step actually emitted"*, but its tab names were guessed (`"DNC List"`, `"Notes"`) — the fix hadn't run in production when it was written. Now that I have the real output, I replaced them with the verbatim production strings (`ca5f13e`, PR #276). A test whose whole claim is "nobody made these up" has to be true about that. Live and verified: `ca5f13e` on the direct App Service URL.

## Housekeeping

`QUEUE.md` row 28 → `DONE 36` with the findings. I ran the **real watcher parser** against the live file rather than assuming: 33 rows, **0 unparsed**, row 28 parses correctly with its item text intact. Your relay scratch files are restored untouched.

Two things found and deliberately **not** done, both flagged in the row:

- Train Hugger's `Company Names` tab is a suppression kind the app doesn't model.
- The default range assuming `Sheet1` will keep failing for any single-tab Sheet not named that. Reading the *first* tab instead is the real fix, but it changes blocklist **content** for all 34 sources — that wants its own cycle and its own red-first test, not a scope-widening at the end of this one.

**Open questions: 2** — (1) should the default range read the first tab rather than assume `Sheet1`, given it changes what lands in 34 blocklists? (2) is `Company Names` a suppression kind worth modelling, or does Train Hugger maintain it for their own use?

