/** Shown when `isPrimary` is still true in the DB but the mailbox is not connected. */
export const MAILBOX_PRIMARY_DISCONNECTED_WARNING =
  "This mailbox is primary but disconnected. Choose a connected mailbox as primary before sending.";
